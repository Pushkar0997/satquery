# Copy to .env and fill. Never commit the filled version.
# No paid API is in scope for the 2-day prototype (see spec/decisions.md D-003).
# If one gets added later (e.g. an LLM key), it goes here:
LLM_API_KEY=
